#include <stdio.h>

int main(void)
{
	int a=100;
	int b=25;
	double d=20;
	float f=15;
	int computation;
	
		computation = a*d/(a-b)+d;
			printf("The computation is: a*d/(a-b)+d = %d\n", computation);

		return(0);
}
